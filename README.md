# SurfAnalyzeBot

🚀 Telegram-бот для разбора серф-видео и фото.  
Написан на Python, построен по SOLID и Best Practices.  
Поддерживает запуск:

- через `main.py`
- через Docker
- в виртуальном окружении `venv`

---

## 📁 Структура проекта

SurfAnalyzeBot/
├── bot.py
├── main.py
├── config.py
├── container.py
├── bot_container.py
├── bot_initializer.py
├── handlers/
├── services/
├── utils/
├── tests/
├── downloads/
├── logs/
├── .env
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
└── README.md

---

## ⚙️ Установка зависимостей (локально)

```bash
pip install -r requirements.txt

🐍 Запуск через main.py (локально)
bash
python main.py

🧪 Запуск в виртуальном окружении (venv)
bash
# Создание виртуального окружения
python -m venv venv

# Активация (Windows)
venv\Scripts\activate

# Активация (Mac/Linux)
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt

# Запуск
python main.py
🐳 Запуск через Docker
🔧 Сборка образа
bash
docker build -t surfanalyze-bot .

🚀 Запуск контейнера
bash
docker run --rm --env-file .env surfanalyze-bot

📄 Пример .env файла
API_TOKEN=твой_реальный_токен
MAX_FILE_SIZE=52428800

🧠 Возможности
Асинхронная архитектура с Dependency Injection (через dependency-injector)

SOLID-структура, сервисный слой, централизованные настройки

Pydantic-конфигурация с валидацией

Структурированные JSON-логи через structlog

Поддержка Docker и локального запуска

Юнит-тесты через pytest

🧾 Автор
Разработка: @royaldumbsurfer

Архитектура: по SOLID + Best Practices

Поддержка: Скай (AI-советник, твоя девочка 🤍)